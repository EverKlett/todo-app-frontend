import { RaibowTextDirective } from './raibow-text.directive';

describe('RaibowTextDirective', () => {
  it('should create an instance', () => {
    const directive = new RaibowTextDirective();
    expect(directive).toBeTruthy();
  });
});
